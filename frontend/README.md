# DatumLex frontend

React dashboard built with Vite and Tailwind CSS.

## Development

```bash
npm install
npm run dev
```

The initial view reproduces the approved appeal-grant-rate dashboard mockup.
Until the API is available, indicators display `-` and the chart shows an
explicit empty state.
